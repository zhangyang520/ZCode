<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.jianhua.zcode.assets">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.CAMERA" /> <!--android.permission.VIBRATE允许访问震动权限-->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>


    <application
            android:name=".data.common.ZCodeApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/app_logo1"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">

        <!-- 目标高度 -->
        <meta-data
                android:name="design_width"
                android:value="750" />

        <meta-data
                android:name="design_height"
                android:value="1334" />

        <!-- 配置 打印的参数 -->
        <meta-data
                android:name="CURRENT_PRINT"
                android:value="${CURRENT_PRINT_VALUE}" />

        <meta-data
                android:name="JPUSH_PKGNAME"
                android:value="${JPUSH_PKGNAME}"
        ></meta-data>

        <meta-data
                android:name="CONSTANT_UPLOAD_URL"
                android:value="${CONSTANT_UPLOAD_URL_VALUE}" />

        <meta-data
                android:name="CONSTANT_DOWNLOAD_URL"
                android:value="${CONSTANT_URL_DOWNLOAD_VALUE}"
        ></meta-data>

        <!-- 配置 url -->
        <meta-data
                android:name="CONSTANT_URL"
                android:value="${CONSTANT_URL_VALUE}" />

        <activity
                android:name="com.jianhua.zcode.assets.ui.activity.LoginActivity"
                android:windowSoftInputMode="stateAlwaysHidden"
        >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 登录界面-->
        <activity
                android:name=".ui.activity.MainActivity"
                android:launchMode="singleTop"
                android:screenOrientation="portrait"
        ></activity>

        <!-- 扫码的详情的界面 -->
        <activity
                 android:name=".ui.activity.ZCodeDeviceDetailActivity"
                 android:launchMode="singleTop"
                 android:screenOrientation="portrait"
        ></activity>

        <!-- 扫码的界面 -->
        <activity
                android:launchMode="singleTop"
                android:screenOrientation="portrait"
                android:name=".ui.activity.ZCodeScanActivity"></activity>
    </application>

</manifest>